import { ValidateDuplicationDirective } from './validate-duplication.directive';

describe('ValidateDuplicationDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateDuplicationDirective();
    expect(directive).toBeTruthy();
  });
});
